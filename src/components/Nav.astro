---
/* Nav — barra fixa, menu mobile e fio de progresso dourado.
   home=true: âncoras locais (#secção). home=false: liga a /#secção (volta à home).
   solid=true: nav já com fundo (páginas internas sem hero). */
import { nav, site } from '@/data/site';

interface Props {
  home?: boolean;
  solid?: boolean;
}
const { home = true, solid = false } = Astro.props;

// Em páginas internas, âncoras passam a apontar para a home.
const href = (h: string) => (home || !h.startsWith('#') ? h : `/${h}`);
const markHref = home ? '#topo' : '/';
---

<nav id="nav" class={solid ? 'stuck' : undefined}>
  <a href={markHref} class="mark">{site.name}</a>
  <button class="burger" id="burger" aria-label="Menu" aria-expanded="false" aria-controls="links">
    Menu
  </button>
  <div class="links" id="links">
    {nav.map((item) => <a href={href(item.href)}>{item.label}</a>)}
  </div>
</nav>
<div id="prog" aria-hidden="true"></div>

<style>
  nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 80;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 26px 48px;
    background: transparent;
    border-bottom: 1px solid transparent;
    transition:
      padding 0.5s var(--ease),
      background 0.5s var(--ease),
      border-color 0.5s var(--ease);
  }
  /* backdrop-filter num pseudo (não no <nav>): assim o <nav> NÃO vira
     containing block dos filhos position:fixed — o menu mobile (.links)
     continua preso à viewport inteira em vez da altura do nav. */
  nav::before {
    content: '';
    position: absolute;
    inset: 0;
    z-index: -1;
    pointer-events: none;
    opacity: 0;
    backdrop-filter: blur(14px) saturate(1.3);
    -webkit-backdrop-filter: blur(14px) saturate(1.3);
    transition: opacity 0.5s var(--ease);
  }
  nav.stuck::before {
    opacity: 1;
  }
  nav.stuck {
    padding: 15px 48px;
    background: rgba(8, 10, 15, 0.72);
    border-bottom-color: rgba(159, 184, 212, 0.14);
  }
  .mark {
    font-family: var(--title);
    font-size: 27px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--bone);
    text-decoration: none;
    white-space: nowrap;
  }
  .links {
    display: flex;
    gap: 24px;
    align-items: center;
  }
  .links a {
    color: var(--bone);
    text-decoration: none;
    font-size: 21px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    opacity: 0.6;
    transition:
      opacity 0.4s,
      color 0.4s;
  }
  .links a:hover {
    opacity: 1;
    color: var(--gold);
  }

  /* fio de progresso */
  #prog {
    position: fixed;
    top: 0;
    left: 0;
    height: 1px;
    width: 100%;
    transform: scaleX(0);
    transform-origin: left center;
    background: var(--gold);
    z-index: 81;
    opacity: 0;
    transition: opacity 0.5s;
    will-change: transform;
  }
  nav.stuck ~ #prog {
    opacity: 1;
  }

  .burger {
    display: none;
    background: none;
    border: 0;
    color: var(--bone);
    font-size: 19px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    cursor: pointer;
  }

  /* Com os links maiores (+8px), o nav horizontal deixa de caber antes de ~1100px:
     colapsa para o menu de hambúrguer já a partir daí. */
  @media (max-width: 1100px) {
    nav {
      padding: 18px 22px;
    }
    nav.stuck {
      padding: 12px 22px;
    }
    .links {
      position: fixed;
      /* Altura explícita (não `inset:0`): quando o nav fica .stuck com
         backdrop-filter, ele vira containing block dos filhos fixed — com
         `inset:0` o overlay herdava a altura do nav (~60px) e o
         translateY(-100%) não o escondia. 100dvh garante o ecrã inteiro. */
      top: 0;
      left: 0;
      right: 0;
      height: 100vh;
      height: 100dvh;
      background: rgba(8, 10, 15, 0.97);
      flex-direction: column;
      justify-content: center;
      gap: 30px;
      transform: translateY(-100%);
      transition: transform 0.6s var(--ease);
    }
    .links.open {
      transform: none;
    }
    .links a {
      font-size: 22px;
      letter-spacing: 0.16em;
      opacity: 1;
    }
    .burger {
      display: block;
      z-index: 85;
    }
  }
</style>
